---
max_turns: 12
allowed_tools: [Read, Glob, Grep, Skill]
---

Rails 8 app with a `Book` model. I need an endpoint so an author can publish a book, and another
to unpublish it. Show me the routes and the controller.
